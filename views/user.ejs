<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Infinite Horizons</title>
    <link rel="stylesheet" href="css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"></script>
</head>
<body>
    <div class="header">
        <div class="upper">
        <nav class="navbar navs navbar-custom navbar-expand-lg pb-0">
          <div class="container-fluid">
            <a class="navbar-brand" href="#">Infinite Horizons</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
              aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
              <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                  <a class="item nav-link active" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                  <a class="item1 nav-link" href="#">Services</a>
                </li>
                <li class="nav-item">
                  <a class="item1 nav-link" href="#">Pricing</a>
                </li>
                <li class="nav-item">
                  <a class="item1 nav-link" href="/signup">Sign Up</a>
                </li>
              </ul>
            </div>
          </div>
        </nav>
        <hr class="rule">
        </div>
    <div class="container mt-5">
        <form action="/search" method="post" class="mb-3">
            <div class="input-group">
                <input type="text" class="form-control" name="searchArea" id="searchArea" placeholder="Search">
                <button type="submit" class="btn btn-primary">Submit</button>
            </div>
        </form>
    
        <% if(locals.flatList) { %>
            <% flatList.forEach(function(item) { %>
                <div class="card">
                    <div class="card-body">
                        <h3><%= item.name %></h3>
                        <h5><%= item.area %></h5>
                        <h6><%= item.address %></h6>
                        <div class="row">
                            <div class="col-4">
                                <h6><b>Rent:</b> <%= item.rent %>
                            </div>
                            <div class="col-4">
                                <b>Floor Area:</b> <%= item.floorArea %></h6>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-4">
                                <h6><strong>Owner Name:</strong> <%= item.ownerDetails.ownerName%>
                            </div>
                            <div class="col-4">
                                <strong>Contact No:</strong> <%= item.ownerDetails.contact %></h6>
                            </div>
                        </div>
                    </div>
                  </div>
            <% }) %>
        <% } else { %>
            <p>No results found.</p>
        <% } %>
    </div> 
</body>









